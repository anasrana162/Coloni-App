import RNDateTimePicker from '@react-native-community/datetimepicker';

export {RNDateTimePicker as CustomDateTimePicker};

type DateTimePickerMode = 'date' | 'time' | 'datetime' | 'countdown';

export type {DateTimePickerMode};
